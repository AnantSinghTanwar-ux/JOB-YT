# Resume parser service config
# Optional shared secret. If set, backend must send it as X-API-Key.
RESUME_PARSER_API_KEY=

# Maximum upload size in bytes (default 10MB)
RESUME_PARSER_MAX_FILE_SIZE_BYTES=10485760

# Restrict parser to PDF only
RESUME_PARSER_ALLOW_ONLY_PDF=true
